version: '3.8'

services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
       - ./.env:/app/.env:ro
    # 如果需要持久化日志或数据，可以添加以下卷
    # - ./logs:/app/logs